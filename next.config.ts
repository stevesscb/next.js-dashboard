import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  /* config options here */
  // For partial pre rendering - still under development, not ready for production
  // experimental: {
  //   ppr: 'incremental',
  // },
};

export default nextConfig;
